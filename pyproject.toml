[project]
name = "playwright-workflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "exchangelib>=5.6.0",
    "playwright>=1.58.0",
]

[dependency-groups]
dev = [
    "nuitka>=4.0.6",
]

